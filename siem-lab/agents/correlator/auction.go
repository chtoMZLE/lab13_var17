package main

// Аукционный механизм распределения задач реализован в main.go
// через подписку на topics tasks.auction и публикацию в tasks.bids.
// Оркестратор ждёт 500мс, выбирает агента с минимальной cost,
// затем публикует назначение в tasks.assigned.{agent_id}.
